[package]
name = "constr"
version = "0.0.1"
edition = "2021"

[lib]
proc-macro = true

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = 'abort'
strip = true

[dependencies]
proc-macro2 = "1.0.78" # So good they needed a second one
syn = "2.0.48"
